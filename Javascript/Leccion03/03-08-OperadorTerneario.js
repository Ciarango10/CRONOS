/* La primera opcion es lo que se hace en caso de ser verdadero
la segunda es en caso de que sea falso.*/

let resultado = (3>2) ? "Verdadero" : 0;
console.log(resultado);

//Ejercicio numero par

let numero = 9;
resultado = (numero % 2 == 0) ? "Es un numero par" : "Es un numero impar";
console.log(resultado);
